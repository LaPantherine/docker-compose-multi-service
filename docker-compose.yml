version: "3"

services:

  app:
    build: ./app
    container_name: python_app

  nginx:
    image: nginx:latest
    container_name: nginx_server
    ports:
      - "8083:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
